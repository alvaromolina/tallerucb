<%= form_for(@comments_solution) do |f| %>
  <% if @comments_solution.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@comments_solution.errors.count, "error") %> prohibited this comments_solution from being saved:</h2>

      <ul>
      <% @comments_solution.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <%= f.hidden_field :solution_id %>
  <div class="field">
    <%= f.label :comment %><br />
    <%= f.text_area :comment %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
